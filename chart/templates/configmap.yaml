apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "vpa-manager.labels" . | indent 4 }}
data:
  TARGET_NAMESPACES: '{{ .Values.targetNamespaces | default (list) | join "," }}'
  {{- range $name, $value := .Values.resourcesToManage }}
  ENABLE_{{ $name | upper }}: {{ $value | quote }}
  {{- end }}
